# NOTE: Cloudflare Pages _redirects can only proxy (200) to internal paths, not to
# an external workers.dev domain — so we do NOT proxy /api here. Instead the
# dashboard fetches the Worker directly using window.F1_WORKER_URL (config.js);
# the Worker sends Access-Control-Allow-Origin: * so cross-origin fetch works.
#
# (Intentionally left without an /api/* rule.)
